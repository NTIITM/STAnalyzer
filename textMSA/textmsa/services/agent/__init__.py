"""
Project Agent service package.

Note: submodules deliberately aren't imported here to avoid circular imports.
Import needed symbols from their concrete modules, e.g.
`from textmsa.services.agent.job_service import get_agent_job_service`.
"""
