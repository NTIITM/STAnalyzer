version: '3.8'

services:
  spatial-imputation-spage-service:
    image: spatial-imputation-spage-service:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spatial-imputation-spage-service
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - OUTPUT_DIR=/app/outputs
    volumes:
      - ./outputs:/app/outputs
    networks:
      - services-network
    restart: unless-stopped

networks:
  services-network:
    driver: bridge